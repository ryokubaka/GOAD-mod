{
  "name": "endpoint-1",
  "namespace": "default",
  "description": "",
  "force": true,
  "package": {
    "name": "endpoint",
    "version": "{{ elastic_defend_resolved_version | default(elastic_defend_version) }}"
  },
  "policy_id": "agent-policy",
  "inputs": [
    {
      "type": "endpoint",
      "enabled": true,
      "streams": [],
      "config": {
        "integration_config": {
          "value": {
            "type": "endpoint",
            "endpointConfig": {
              "preset": "EDRComplete"
            }
          }
        }
      }
    }
  ]
}