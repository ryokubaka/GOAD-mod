# so_elastic_security (GOAD-mod)

On the Security Onion manager after `securityonion` role: Elastic trial, Elastic
Defend (`EDRComplete` / detect), enable prepackaged detection rules.

Ported from ludus-source-meow `ludus_so_elastic_security`.
