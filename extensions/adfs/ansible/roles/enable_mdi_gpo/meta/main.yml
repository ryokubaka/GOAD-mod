---
dependencies: []

galaxy_info:
  role_name: enable_mdi_gpo
  author: "@curi0usJack"
  description: An Ansible Role that configures GPO audit settings for Microsoft Defender for Identity
  company: TrustedSec
  license: GPLv3
  min_ansible_version: "2.10"
  platforms:
    # Modify the platforms here as required
    # This example includes a lot, see https://github.com/ansible/ansible-lint/blob/main/src/ansiblelint/schemas/meta.json for all possibilities
    - name: Windows 
      versions:
        - "2012R2"
        - "2016"
        - "2019"
        - "2022"

collections: [ansible.windows.win_powershell] # Add any collections needed here
